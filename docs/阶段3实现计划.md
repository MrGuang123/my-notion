# 阶段3：任务与通知 - 实现计划

## 📋 概述

实现任务管理系统和异步通知系统，使用 BullMQ 队列处理任务事件，自动生成通知记录。

---

## 🎯 实现目标

### 核心功能
1. **任务管理**: 创建、查询、更新、删除任务，支持状态流转和人员指派
2. **通知系统**: 异步生成通知，支持未读/已读状态
3. **队列处理**: 使用 BullMQ 处理任务事件，解耦任务操作和通知生成

### 技术要点
- TypeORM 实体设计（JSONB 存储 assignees 和 payload）
- 状态机模式（任务状态流转校验）
- BullMQ 生产者/消费者模式
- 租户隔离（所有数据带 tenantId）

---

## 📝 任务清单

### 第一步：修复阶段1的Bug（优先）
**文件**: `src/modules/auth/auth.service.ts`
- [ ] 修复第41行的注册逻辑 `if (!existing)` → `if (existing)`

**预计耗时**: 1分钟

---

### 第二步：创建任务模块实体和DTO

#### 2.1 创建任务实体
**文件**: `src/modules/tasks/entities/task.entity.ts`
- [ ] 定义 Task 实体
  - id (number, 自增主键)
  - tenantId (string)
  - title (string, max 200)
  - description (text, nullable)
  - status (enum: todo/doing/done)
  - assignees (jsonb, string[], 默认 [])
  - dueDate (timestamp, nullable)
  - createdBy (string, nullable)
  - createdAt (timestamp)
  - updatedAt (timestamp)

#### 2.2 创建任务状态枚举
**文件**: `src/modules/tasks/task-status.enum.ts`
- [ ] 定义 TaskStatus enum (todo, doing, done)

#### 2.3 创建任务DTO
**文件**: `src/modules/tasks/dtos/create-task.dto.ts`
- [ ] title: string (必填, max 200)
- [ ] description?: string (可选)
- [ ] assignees?: string[] (可选, 默认 [])
- [ ] dueDate?: Date (可选)

**文件**: `src/modules/tasks/dtos/update-task.dto.ts`
- [ ] status?: TaskStatus (可选)
- [ ] title?: string (可选, max 200)
- [ ] description?: string (可选)
- [ ] assignees?: string[] (可选)
- [ ] dueDate?: Date (可选)

**文件**: `src/modules/tasks/dtos/task-query.dto.ts`
- [ ] page?: number (默认 1)
- [ ] pageSize?: number (默认 20)
- [ ] status?: TaskStatus (可选, 过滤状态)
- [ ] assignee?: string (可选, 过滤指派人)

**预计耗时**: 15分钟

---

### 第三步：创建通知模块实体和DTO

#### 3.1 创建通知实体
**文件**: `src/modules/notifications/entities/notification.entity.ts`
- [ ] 定义 Notification 实体
  - id (number, 自增主键)
  - userId (string)
  - type (enum: task_assigned|comment_reply|custom)
  - payload (jsonb, Record<string, unknown>)
  - read (boolean, 默认 false)
  - createdAt (timestamp)

#### 3.2 创建通知类型枚举
**文件**: `src/modules/notifications/notification-type.enum.ts`
- [ ] 定义 NotificationType enum (task_assigned, comment_reply, custom)

#### 3.3 创建通知DTO
**文件**: `src/modules/notifications/dtos/notification-query.dto.ts`
- [ ] page?: number (默认 1)
- [ ] pageSize?: number (默认 20)
- [ ] unreadOnly?: boolean (可选, 默认 false)

**预计耗时**: 10分钟

---

### 第四步：实现任务服务和业务逻辑

#### 4.1 创建任务服务
**文件**: `src/modules/tasks/tasks.service.ts`
- [ ] create(tenantId, userId, dto): 创建任务
  - 保存到数据库
  - 如果有 assignees，发送任务分配事件到队列
- [ ] list(tenantId, query): 查询任务列表
  - 支持分页
  - 支持按 status 过滤
  - 支持按 assignee 过滤（使用 JSONB 查询）
- [ ] findOne(tenantId, id): 查询单个任务
- [ ] update(tenantId, id, userId, dto): 更新任务
  - 状态流转校验（todo → doing → done，允许回退）
  - 如果 assignees 发生变化，发送任务分配事件
- [ ] remove(tenantId, id): 删除任务

#### 4.2 状态流转校验逻辑
- [ ] 允许的流转：
  - todo → doing
  - doing → done
  - done → doing (允许回退)
  - doing → todo (允许回退)
- [ ] 不允许：todo → done (直接跳过 doing)

**预计耗时**: 30分钟

---

### 第五步：实现通知服务

#### 5.1 创建通知服务
**文件**: `src/modules/notifications/notifications.service.ts`
- [ ] create(userId, type, payload): 创建通知
- [ ] list(userId, query): 查询用户通知列表
  - 支持分页
  - 支持只查询未读（unreadOnly=true）
- [ ] markAsRead(userId, id): 标记为已读
  - 校验通知属于该用户
- [ ] getUnreadCount(userId): 获取未读数量（可选）

**预计耗时**: 15分钟

---

### 第六步：实现队列生产者和消费者

#### 6.1 定义任务事件类型
**文件**: `src/modules/tasks/events/task-event.interface.ts`
- [ ] 定义 TaskEventType enum (task_created, task_assigned, task_updated)
- [ ] 定义 TaskEvent interface
  - type: TaskEventType
  - tenantId: string
  - taskId: number
  - actorId?: string
  - assignees?: string[]
  - previousAssignees?: string[]

#### 6.2 在任务服务中集成队列
**文件**: `src/modules/tasks/tasks.service.ts`
- [ ] 注入 QueueService
- [ ] 在 create() 中：如果有 assignees，发送 task_assigned 事件
- [ ] 在 update() 中：如果 assignees 变化，发送 task_assigned 事件

#### 6.3 创建任务队列消费者
**文件**: `src/modules/tasks/tasks.processor.ts`
- [ ] 创建 @Processor('default') 类
- [ ] 处理 task_assigned 事件：
  - 获取新增的 assignees（与 previousAssignees 比较）
  - 为每个新 assignee 创建通知
  - 通知类型：task_assigned
  - payload: { taskId, taskTitle, assignedBy, assignedAt }

**预计耗时**: 25分钟

---

### 第七步：实现任务和通知控制器

#### 7.1 任务控制器
**文件**: `src/modules/tasks/tasks.controller.ts`
- [ ] POST /tasks - 创建任务
- [ ] GET /tasks - 查询任务列表（支持过滤）
- [ ] GET /tasks/:id - 查询单个任务
- [ ] PATCH /tasks/:id - 更新任务（状态/指派）
- [ ] DELETE /tasks/:id - 删除任务
- [ ] 所有接口使用 @UseGuards(TenantGuard)

#### 7.2 通知控制器
**文件**: `src/modules/notifications/notifications.controller.ts`
- [ ] GET /notifications - 查询通知列表（支持 unreadOnly）
- [ ] PATCH /notifications/:id/read - 标记为已读
- [ ] 不需要 TenantGuard（通知是用户维度，不是租户维度）

**预计耗时**: 20分钟

---

### 第八步：创建模块并注册

#### 8.1 任务模块
**文件**: `src/modules/tasks/tasks.module.ts`
- [ ] 导入 TypeOrmModule.forFeature([Task])
- [ ] 导入 QueueModule
- [ ] 导入 NotificationsModule
- [ ] 注册 TasksService, TasksController, TasksProcessor
- [ ] 导出 TasksService

#### 8.2 通知模块
**文件**: `src/modules/notifications/notifications.module.ts`
- [ ] 导入 TypeOrmModule.forFeature([Notification])
- [ ] 注册 NotificationsService, NotificationsController
- [ ] 导出 NotificationsService

#### 8.3 在 AppModule 中注册
**文件**: `src/app.module.ts`
- [ ] 导入 TasksModule
- [ ] 导入 NotificationsModule

**预计耗时**: 10分钟

---

### 第九步：编写测试（可选，先实现功能）

#### 9.1 单元测试
**文件**: `src/modules/tasks/tasks.service.spec.ts`
- [ ] 测试状态流转校验逻辑
- [ ] 测试任务创建/更新

**文件**: `src/modules/tasks/tasks.processor.spec.ts`
- [ ] 测试队列处理器生成通知

#### 9.2 e2e 测试
**文件**: `test/tasks-notifications.e2e-spec.ts`
- [ ] 测试：创建任务 → 指派用户 → 查询通知 → 标记已读
- [ ] 测试：更新任务状态 → 校验状态流转
- [ ] 测试：更新 assignees → 校验新通知生成

**预计耗时**: 40分钟（可后置）

---

## 📂 目录结构（新增）

```
src/modules/
├── tasks/
│   ├── entities/
│   │   └── task.entity.ts
│   ├── dtos/
│   │   ├── create-task.dto.ts
│   │   ├── update-task.dto.ts
│   │   └── task-query.dto.ts
│   ├── events/
│   │   └── task-event.interface.ts
│   ├── task-status.enum.ts
│   ├── tasks.controller.ts
│   ├── tasks.service.ts
│   ├── tasks.processor.ts
│   └── tasks.module.ts
└── notifications/
    ├── entities/
    │   └── notification.entity.ts
    ├── dtos/
    │   └── notification-query.dto.ts
    ├── notification-type.enum.ts
    ├── notifications.controller.ts
    ├── notifications.service.ts
    └── notifications.module.ts
```

---

## ⏱️ 预计总耗时

| 任务 | 耗时 |
|------|------|
| 修复 Bug | 1 分钟 |
| 创建实体和 DTO | 25 分钟 |
| 实现服务逻辑 | 45 分钟 |
| 实现队列处理 | 25 分钟 |
| 实现控制器 | 20 分钟 |
| 创建模块并注册 | 10 分钟 |
| **总计（不含测试）** | **约 2 小时** |
| 编写测试（可选） | 40 分钟 |

---

## 🔑 关键技术点

### 1. JSONB 查询（assignees 过滤）
```typescript
// 查询包含某个 userId 的任务
.where(`tasks.assignees @> :assignees`, { 
  assignees: JSON.stringify([userId]) 
})
```

### 2. 状态流转校验
```typescript
const validTransitions: Record<TaskStatus, TaskStatus[]> = {
  [TaskStatus.Todo]: [TaskStatus.Doing],
  [TaskStatus.Doing]: [TaskStatus.Todo, TaskStatus.Done],
  [TaskStatus.Done]: [TaskStatus.Doing],
};

if (!validTransitions[currentStatus]?.includes(newStatus)) {
  throw new BadRequestException('Invalid status transition');
}
```

### 3. 队列事件发送
```typescript
await this.queueService.addJob('task.assigned', {
  tenantId,
  taskId: task.id,
  assignees: task.assignees,
});
```

### 4. 队列消费者
```typescript
@Processor('default')
export class TasksProcessor {
  @Process('task.assigned')
  async handleTaskAssigned(job: Job<TaskEvent>) {
    // 生成通知
  }
}
```

---

## ⚠️ 注意事项

1. **租户隔离**: 任务受 TenantGuard 保护，通知是用户维度（不需要 tenantId）
2. **并发安全**: assignees 更新时需要对比 previousAssignees，只为新增用户发通知
3. **队列重试**: BullMQ 默认支持重试，消费者需要幂等性（可通过 notification 去重）
4. **性能考虑**: 
   - 通知列表查询使用索引 (userId, createdAt)
   - 未读数量可以考虑缓存
5. **扩展性**: 
   - NotificationType 预留 comment_reply 类型（阶段2可扩展）
   - payload 使用 JSONB，可存储任意结构数据

---

## 🎯 验收标准

### 功能验收
- [ ] 可以创建任务，指定 assignees
- [ ] 可以查询任务列表，支持按 status/assignee 过滤
- [ ] 可以更新任务状态，非法流转被拒绝
- [ ] 可以更新 assignees，新增用户收到通知
- [ ] 可以查询通知列表，支持只看未读
- [ ] 可以标记通知为已读

### 技术验收
- [ ] 任务事件成功入队
- [ ] 队列消费者成功处理事件
- [ ] 通知记录成功落库
- [ ] 所有接口遵循 RESTful 规范
- [ ] DTO 校验完整
- [ ] 租户隔离正确

### 测试验收（可选）
- [ ] e2e 测试覆盖完整流程
- [ ] 单元测试覆盖状态流转和队列处理

---

## 📌 实施顺序

**推荐按以下顺序实施**（最小化依赖，便于增量验证）：

1. 修复 Bug（1 分钟）
2. 创建任务实体 + DTO（10 分钟）→ 可以先测数据库
3. 创建通知实体 + DTO（10 分钟）→ 可以先测数据库
4. 实现通知服务 + 控制器（20 分钟）→ 可以先测通知功能
5. 实现任务服务（基础 CRUD，30 分钟）→ 可以先测任务功能
6. 集成队列（生产者 + 消费者，25 分钟）→ 测试完整流程
7. 补充测试（可选，40 分钟）

---

**准备就绪，等待您的确认后开始实施！** 🚀
